[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120 medantaclone.wsgi:application"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
